import React from 'react'
import { useSelector } from 'react-redux'

function News() {
    const state = useSelector((state) => state);
    return (
        <div><h1>

        </h1> </div>
    )
}

export default News